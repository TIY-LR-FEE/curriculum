var camelCase = "Camel Case is when you start with a lower case letter, but write each new word with an uppercase letter."
console.log(camelCase);

// Wait for Output

var age = 5;

if (age >= 5) {
	console.log("Conditions can use variables");
}

// Wait for Output

var firstName = "Your First Name Here";
var lastName = "Your Last Name Here";
var name = firstName + " " + lastName;
console.log("Your name is: " + name);

// Wait for Output

var collection = [1, 2, 3, 4]
console.log(collection);

// Wait for Output

function add(a, b) {
	return a + b;
}

console.log(add(2, 2));

// Wait for Output
